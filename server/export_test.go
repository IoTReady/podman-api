package server

// Test-only aliases for unexported functions. Only compiled during tests.
var BuildJobRegistry = buildJobRegistry
var ComposeHandler = composeHandler
var OpenStore = openStore
var SeedTemplates = seedTemplates
